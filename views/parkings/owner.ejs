<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Owner</title>
</head>

<body>
    <h2> Owner Info </h2>
    <p>Name: <%= owner.name %>
    </p>
    <p>Email: <%= owner.email %>
    </p>
    <p>Phone: <%= owner.phone %>
    </p>
    <hr><br>
    <h2>Your Parking Lots: </h2>
    <% for (const parking of owner.parkings) { %>
    <% if(parking.owner.equals(owner._id)) { %>
        <div>
            <div>
                Parking Name: <%= parking.name %>
            </div>
            <div>
                Location: <%= parking.location %>
            </div>
            <div>
                Rate per Hour: ₹<%= parking.rate %>
            </div>
            <a href="/parkings/owner/<%= owner._id %>/<%= parking._id %>">Details</a>
        </div><br>
    <% } %>
    <% } %>
        <hr>
        <br>
        <b>Create Parking: </b><a href="/parkings/owner/<%= owner._id %>/new">Click here</a>
</body>

</html>